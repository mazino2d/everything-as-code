variable "grafana_prometheus_datasource_uid" {
  description = "UID of the Prometheus datasource in the mazino2d stack. Find it in Connections > Data Sources > Prometheus > UID field."
  type        = string
  default     = null
}
