apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: apps
helmGlobals:
  chartHome: ../../../charts
resources:
  - namespace.yaml
  - hotrod
  - httpbin
helmCharts:
  - name: eac-redis
    releaseName: redis
    valuesFile: redis.yml
