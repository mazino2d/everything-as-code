# Allow both mTLS and plaintext traffic for all workloads in the mesh.
# Applying this to istio-system makes it a mesh-wide policy.
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default
  namespace: istio-system
spec:
  mtls:
    mode: PERMISSIVE
