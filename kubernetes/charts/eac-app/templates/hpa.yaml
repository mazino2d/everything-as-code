{{- if and (eq .Values.workload "deployment") .Values.deployment.autoscaling.enabled }}
{{- $argoStrategies := list "BlueGreen" "ReplicaCanary" "MeshTrafficCanary" }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    {{- if has .Values.deployment.rollout.strategy $argoStrategies }}
    apiVersion: argoproj.io/v1alpha1
    kind: Rollout
    {{- else }}
    apiVersion: apps/v1
    kind: Deployment
    {{- end }}
    name: {{ .Values.name }}
  minReplicas: {{ .Values.deployment.autoscaling.minReplicas | default 1 }}
  maxReplicas: {{ .Values.deployment.autoscaling.maxReplicas | default 3 }}
  metrics:
    {{- if .Values.deployment.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.deployment.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.deployment.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.deployment.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
