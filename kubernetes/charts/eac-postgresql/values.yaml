# Cluster-wide Infisical defaults (can be overridden per instance under infisical:)
infisical:
  projectSlug: ""
  envSlug: dev
  hostAPI: https://app.infisical.com/api
  resyncInterval: 60
  credentialsRef:
    secretName: infisical-machine-identity
    secretNamespace: infra

instances: []
# Each entry supports:
#   name: ""                    (required)
#   replicaCount: 1              # single instance for now; keep = 1
#   image:
#     repository: postgres
#     tag: "16-alpine"
#     pullPolicy: IfNotPresent
#   auth:
#     adminUsername: postgres
#     password:
#       source: auto             # auto | existingSecret
#       secretName: ""          # optional; default: <instance-name>-auth
#       existingSecret: ""      # required when source=existingSecret
#       infisical:
#         enabled: true
#         projectSlug: everything-as-code
#         envSlug: dev
#         secretsPath: /apps/app-db  # folder path in Infisical
#         resyncInterval: 60
#         hostAPI: https://app.infisical.com/api
#         credentialsRef:
#           secretName: infisical-machine-identity
#           secretNamespace: infra
#   storage:
#     size: 1Gi
#     storageClassName: ""      # empty = cluster default
#     mountPath: /var/lib/postgresql/data
#   service:
#     type: ClusterIP            # ClusterIP | NodePort | LoadBalancer
#     port: 5432
#     nodePort: null             # used only when type = NodePort
#   resources:
#     requests:
#       cpu: 50m
#       memory: 128Mi
#     limits:
#       memory: 512Mi
#   securityContext:
#     runAsUser: 999
#     runAsGroup: 999
#     fsGroup: 999
#   probes:
#     startup:
#       enabled: true
#       initialDelaySeconds: 10
#       periodSeconds: 10
#       timeoutSeconds: 5
#       failureThreshold: 30
#     readiness:
#       initialDelaySeconds: 10
#       periodSeconds: 10
#       timeoutSeconds: 5
#       failureThreshold: 6
#     liveness:
#       initialDelaySeconds: 30
#       periodSeconds: 20
#       timeoutSeconds: 5
#       failureThreshold: 6
#   postgresql:
#     maxConnections: "100"
#     sharedBuffers: "128MB"
#     workMem: "4MB"
#     maintenanceWorkMem: "64MB"
#     walLevel: "replica"
#   atlas:
#     enabled: false
#     name: hotrod-db-schema   # AtlasSchema resource name for this database/app
#     # New preferred form for multiple AtlasSchema resources per DB instance:
#     # schemas:
#     #   - name: hotrod-db-schema
#     #     schemaConfigMap: hotrod-db-schema
#     #     schemas: [public]
#     #     exclude: ["public.httpbin_*"]
#     #   - name: httpbin-db-schema
#     #     schemaConfigMap: httpbin-db-schema
#     #     schemas: [public]
#     #     exclude: ["public.hotrod_*"]
#     # The legacy fields below still work for a single AtlasSchema.
#     # Optional explicit URL inputs. If omitted, the chart derives Atlas credentials
#     # from the DB instance config and auth secret.
#     # url: postgres://...
#     # urlFrom:
#     #   secretKeyRef:
#     #     name: app-db-auth
#     #     key: url
#     # credentials:
#     #   scheme: postgres
#     #   host: app-db.apps.svc.cluster.local
#     #   port: 5432
#     #   database: postgres
#     #   user: postgres
#     #   passwordFrom:
#     #     secretKeyRef:
#     #       name: app-db-auth   # Optional; defaults to auth.password.secretName or <instance>-auth
#     #       key: admin_password # Optional; defaults to admin_password
#     schemaConfigMap: hotrod-db-schema # Optional string form; key defaults to schema.hcl
#     # schemaConfigMap:
#     #   name: hotrod-db-schema       # Optional object form; defaults to atlas.name
#     #   key: schema.hcl              # Optional; defaults to schema.hcl
#     # schemas: [public]              # Optional DB schema scope for this AtlasSchema
#     # exclude: ["public.audit_*"]   # Optional object exclusion patterns
#     policy:
#       lint:
#         destructive:
#           error: true        # Reject destructive schema changes
#   exporter:
#     enabled: false
#     image:
#       repository: quay.io/prometheuscommunity/postgres-exporter
#       tag: "v0.15.0"
#       pullPolicy: IfNotPresent
#     port: 9187
#     resources:
#       requests:
#         cpu: 10m
#         memory: 32Mi
#       limits:
#         memory: 64Mi
#   networkPolicy:
#     enabled: false
#     allowFromNamespaces: []    # e.g. ["apps", "monitoring"]
#   backup:
#     enabled: false
#     schedule: "0 2 * * *"
#     successfulJobsHistoryLimit: 3
#     failedJobsHistoryLimit: 3
#     restartPolicy: OnFailure
#     storage:
#       enabled: false
#       existingClaim: ""       # required when storage.enabled = true
#       mountPath: /backup
#     # Optional command override, example:
#     # command:
#     #   - /bin/sh
#     #   - -c
#     #   - pg_dump -h 127.0.0.1 -U "$POSTGRES_USER" -d "$POSTGRES_DB" | gzip > /backup/dump.sql.gz
